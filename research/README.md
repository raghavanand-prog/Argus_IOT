# research/ — not yet populated

The original planning bundle specifies `paper-outline.md`, `experiment-plan.md`, and
`baselines.md` here (see the full text preserved in the master plan discussion, and
condensed in `docs/04-evaluation-protocol.md`). These are downstream of the evaluation
harness (`eval/harness.py`) having enough real, multi-seed results to write about
honestly — see `STATUS.md`'s "immediate next steps". Writing the paper outline before
there's anything to report would invite exactly the fabricated-numbers failure mode
`CLAUDE.md` rule 4 forbids.
