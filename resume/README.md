# resume/ — not yet populated

Per the original plan's rule (and `CLAUDE.md` rule 4, "no fabricated results"): no
bullet goes here until the number in it exists in `results/`. The current
`eval/harness.py` output is a single-seed, uninformative-F1 demonstration (see
`STATUS.md`) — real enough to prove the loop and the ablation mechanism work, not yet
real enough to quote in a resume bullet. Populate this once `eval/harness.py` produces
multi-seed, properly-labelled metrics.
